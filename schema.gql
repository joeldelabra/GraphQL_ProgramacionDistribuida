type Query {
    receta(id:ID): Receta
    pid: Int
}
type Receta {
    id: ID!
    nombre: String!
    pasos: String
    ingredientes: [ingrediente]!
}
type Ingrediente{
    id:ID!
    nombre:String!
    cantidad: String
}